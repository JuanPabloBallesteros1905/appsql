import 'package:flutter/material.dart';

class Direcciones extends StatelessWidget {
  const Direcciones({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('DIreciones Page'),
    );
  }
}
